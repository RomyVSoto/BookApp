<div class="row mb-3">
  <div class="col-12 col-sm-4">
    <a href="/" class="btn btn-warning"> Back to home </a>
  </div>
</div>

{{#if hasBook}}

<div class="row">

  <div class="col-12 col-sm-4 mx-auto mb-3">
    <div class="card">
      <div class="card-body mx-auto text-center">
        <img src="{{book.imageUrl}}" class="card-img-top" alt="..." height="500px" />
        <h4 class="card-title my-3 fw-bold">{{book.title}}</h4>
        <h5 class="card-title mb-3"><em>Categoria: <br></em>{{book.category.name}}</h5>
        <h5 class="card-title mb-3"><em>Año: <br></em>{{book.year}}</h5>
        <h5 class="card-title mb-3"><em>Autor: <br></em>{{book.author.name}}</h5>
        <h5 class="card-title mb-3"><em>Editorial: <br></em>{{book.editorial.name}}</h5>
      </div>
    </div>
  </div>

</div>

{{else}}

<h1>book not found!</h1>

{{/if}}